package questao4;

import java.util.ArrayList;

public class List {
/*
 * What happens when the following code is executed?
var list = new List<String> { "take", "net" };
for (int i = 0; i < list.Count; i++)
{
list.Add(list[i].ToUpper());
}
	() Changes all list values to
	uppercase.
	 () Keeps the first two items in lowercase and adds new items in
	uppercase.
	(X) Out of memory exception.
	() Only the values "TAKE" and "NET" will be added to the list.
	() Index out of bounds exception.
	
	O código entrará em loop infinito.
	*/
}
